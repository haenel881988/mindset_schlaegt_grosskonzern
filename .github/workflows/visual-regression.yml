name: Visual Regression (disabled)

# Dieser Workflow ist absichtlich deaktiviert – er darf NICHT in CI/PRs laufen.
# Visuelle Regressionstests laufen ausschließlich lokal.

on:
  push:
    branches-ignore: ["**"]
  pull_request:
    branches-ignore: ["**"]
  schedule: []

jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Visual Regression workflow ist deaktiviert und läuft nicht."

---
const navLinks = [
  { href: "/", text: "Home" },
  { href: "/ueber-mich", text: "Über Mich" },
  { href: "/bewerbung", text: "Angebot" },
]
---
<header class="bg-background-primary/80 backdrop-blur-sm sticky top-0 z-50 border-b border-border-default/70">
  <div class="max-w-5xl mx-auto px-4 sm:px-6">
    <div class="flex items-center justify-between h-16">
      <div class="flex-shrink-0">
        <a href="/" class="text-xl font-bold text-text-primary hover:text-accent transition-colors">Simon Haenel</a>
      </div>
      <nav class="hidden md:flex md:space-x-8">
        {navLinks.map(link => (
          <a href={link.href} class="text-base font-medium text-text-secondary hover:text-text-primary transition-colors">
            {link.text}
          </a>
        ))}
      </nav>
    </div>
  </div>
</header>
